import { HomePage } from '@/features/home/components/home-page';

/**
 * Entry point for the application.
 * Renders the home page feature.
 */
export default function Page() {
  return <HomePage />;
}
